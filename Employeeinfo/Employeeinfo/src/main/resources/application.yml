eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 9092
spring:
  application:
    name: Employee-jpastreamer
  datasource:
    url: jdbc:mysql://localhost:3306/microservicesproject
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true